name: fastapi-playground
channels:
  - conda-forge
  - defaults
dependencies:
  - fastapi
  - uvicorn[standard]
  - requests
  - asyncpg
  - sqlmodel
  - pyhumps
  - pip
  # build and test
  - pytest
  - pytest-mock
  - pylint
  - mypy
  - pyyaml
  - pip:
    - humps
