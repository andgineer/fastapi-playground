name: fastapi-playground
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - fastapi
  - uvicorn[standard]
  - requests
  - psycopg2
  - sqlmodel
  - humps
  - pip
  # build and test
  - pytest
  - pytest-mock
  - pylint
  - mypy
  - pyyaml
  - pip:
    - humps
